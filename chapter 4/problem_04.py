# replace the double space from problem 3 with single spaces.

# (problem 03)
# write a program to detect double space in a string.
name="ram is a good  boy and"
print(name.replace("  "," "))  #replace in the double quote(spaces)

print(name)                  #string are immutable which means that you cannot change them by running functions on them


